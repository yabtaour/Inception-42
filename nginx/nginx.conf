
http {
	server {
		ssl on;
		listen 443 ssl;
		ssl_certificate /etc/nginx/ssl/vbachele.crt;
		ssl_certificate_key /etc/nginx/ssl/vbachele.key;	
		ssl_protocols TLSv1.3;

		root /usr/share/nginx/html;
	
		location / {
			root /usr/share/nginx/html/;
		}

		location ~ \.php$  {
			include		fastcgi_params;
			fastcgi_pass	127.0.0.1:9000;
			fastcgi_param	SCRIPT_FILENAME $document_root$fastcgi_scriptname;
			try_files	$uri =404;
			fastcgi_index	index.php;
			
		}
	}
}

events {}

#the localtion is /var/www/html I need to make it by default to /usr/share/nginx/html

